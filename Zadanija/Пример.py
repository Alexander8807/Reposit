#import csv

#with open("TableHome.csv", "r", encoding="UTF8") as homefile:
    #csv_reader = csv.reader(homefile)
    #next(csv_reader)
    #next(csv_reader)

    ##result_list = []
    ##for line in csv_reader:
        # socsup = line[4]
        # socsup = str.split(socsup)
        ##result_list.append([line[0], line[1], line[4]])
        # for num in socsup:
        #     print(socsup)

    ##print(sorted(result_list, key=lambda x: x[2], reverse=True))



















